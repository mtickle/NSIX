@model IEnumerable<NSIX.Models.VehicleModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicleMake.MakeCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModelCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleMake.MakeCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModelCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehicleModelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehicleModelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehicleModelID })
        </td>
    </tr>
}

</table>
