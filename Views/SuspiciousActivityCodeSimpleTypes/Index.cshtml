@model IEnumerable<NSIX.Models.SuspiciousActivityCodeSimpleType>

@{
    ViewBag.Title = "Suspicious Activity Types";
}




<h2>@ViewBag.Title</h2>

<p>
    @{if (Request.IsAuthenticated)
        {
            <input type="button" class="btn btn-default" value="Create New" onclick="location.href='@Url.Action("Create", "SuspiciousActivityCodeSimpleTypes")'" />
        }
    }
</p>

<p>
    </p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FacetValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FacetValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.SuspiciousActivityCodeSimpleTypeID }) |
                @*@Html.ActionLink("Details", "Details", new { id=item.SuspiciousActivityCodeSimpleTypeID }) |*@
                @Html.ActionLink("Delete", "Delete", new { id = item.SuspiciousActivityCodeSimpleTypeID })
            </td>
        </tr>
    }

</table>

<p>
    <input type="button" class="btn btn-default" value="Create New" onclick="location.href='@Url.Action("Create", "SuspiciousActivityCodeSimpleTypes")'" />
</p>